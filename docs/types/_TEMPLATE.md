# 类型 Playbook 模板:<视频类型名>

> 复制本文件为 `docs/types/<类型id>.md` 填写。只写**这个类型独有**的东西;通用手艺见 `../motion-constitution.md`,别重复。
> 填好后:① 把结构/节奏写进 `presets/structures/<类型id>.json` ② 招牌动效做成组件或场景配方。

## 1. 用在哪
用途 / 适配平台 / 典型时长 / 适配画幅。

## 2. 结构公式(节拍表)
这个类型的叙事骨架,带情绪曲线与每拍目的。例:数据片 = 设问 → 揭示 → 反转 → 收束。
| 节拍 role | 目的 | priority | min/ideal/max 秒 | 招牌视觉 |
|---|---|---|---|---|
|  |  |  |  |  |

## 3. 节奏规则
每段几刀、停留多久、Hook 时机、哪里加速/呼吸。

## 4. 动效词汇表(何时用什么)
这个类型该用哪些招牌动效、怎么组合。例:数据片→数字滚动 + 柱状 race + 错峰揭示;知识片→图解逐步揭示 + 标注引线 + 概念 morph。
| 动效 | 用在哪一拍 | 背后原理(为什么有效) |
|---|---|---|

## 5. 排版 / 颜色 / 层级偏好
这个类型的特殊偏好(如数据片要等宽数字字体)。

## 6. 声音 / 卡点
音乐风格、是否卡点、旁白节奏。

## 7. 类型专属反 pattern
这个类型特有的"廉价感"来源(通用反 slop 之外)。

## 8. 参考范例(拆解过的)
3-5 条,带链接 + 时间戳 + 为什么好。详细拆解卡放 `references/teardowns/<类型id>/`。

## 9. 用到的组件 / 场景配方
对应 `remotion/src/templates/` 与 `remotion/src/blocks/` 的清单。
